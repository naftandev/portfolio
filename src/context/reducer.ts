import types from './types'

const reducer = (state, action) => {
  switch (action.type) {
    case types.EXPERIENCE_UPDATE_JOB_SENIORITY:
      return state
    default:
      return state
  }
}

export default reducer
